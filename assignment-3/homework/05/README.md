# calculator.py 说明

## 程序功能

这是一个简单的计算器程序，实现了加法和乘法两个基本运算功能，并输出两个示例运算的结果。

## 函数说明

- `add(a, b)`：加法函数，接收两个参数，返回 `a + b` 的值。
- `multiply(a, b)`：乘法函数，接收两个参数，返回 `a * b` 的值。

## 运行结果

程序依次调用两个函数并打印结果：

```
5
20
```

即 `add(2, 3)` 的计算结果为 5，`multiply(4, 5)` 的计算结果为 20。